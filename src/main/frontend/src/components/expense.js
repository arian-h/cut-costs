import React, { Component } from 'react';

export default class Expense extends Component {
  static getTitle() {
    return 'Expense';
  }
  render() {
    return (
      <div>Expense</div>
    );
  }
}
